doctype html
html
  head

    
    script(type="text/javascript").
        const ROOM_ID = "#{roomId}"
        console.log("Inside  room.pug : " + ROOM_ID);

    script(type="text/javascript", src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js" defer)
    script(type="text/javascript", src="/socket.io/socket.io.js" defer)
    script(type="text/javascript", src="/script.js" defer)
   


    title= GlideSkyHy

    style.
        body {
            background-color: #212529;
            font-family:verdana;
        }

        video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        #video-grid {
        	display: grid;
        	grid-template-columns: repeat(auto-fill, 300px);
        	grid-auto-rows: 300px;
        	grid-gap: 30px;
        }

  body
    a(href='/index.html', style="font-family:verdana; color:white; text-decoration: none; border-style: solid; border-color: white; font-size: x-large;") GlideSkyHy
    h1(style="text-align:center;color:white;") Video Call

    div(id='video-grid')

    <!--p = #{roomId}-->


